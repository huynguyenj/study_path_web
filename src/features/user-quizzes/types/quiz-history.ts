export type QuizHistoryType = {
      id: string | number
      name: string
      correctNumbers: number
      totalQuestion: number
      point: number
      completeAt: string
      passScore: number
}